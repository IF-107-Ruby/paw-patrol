= provide(:button_text, 'Create new member')
= provide(:cancel_btn_text, 'Cancel')
= provide(:path, users_path)
h2.title Create company member
= render 'form', member: @member, company: @company, password_required: true
