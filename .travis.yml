dist: xenial
cache: bundler
language: ruby
script:
  - bundle exec rake
  - bundle exec rubocop --require rubocop-rails
services:
  - postgresql
before_script:
  - cp config/database.yml.travis config/database.yml
